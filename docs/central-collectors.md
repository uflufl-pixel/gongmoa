# 중앙기관 직접 수집 연결 1차

## 8차: 국토교통부

2026-09-02 공식 본부 알림마당 `/USR/BORD0201/m_69/LST.jsp?id=N01_B` 10행에서 수탁기관 재공모 1건을 확인했다. `molit-board`를 batch 2에 추가한다. 공개 서버가 발급한 동일 URL 세션 리다이렉트 쿠키를 1회만 유지하며 다른 URL로 전달하거나 영구 저장하지 않는다. 숫자 문자 참조와 상대 상세 링크를 해석하고 게시판 ID·글 ID를 검증한다. 접수기간은 미확인으로 유지한다. 연결 대상 15개, 미연결 30개이며 과거 전체 목록 수집을 뜻하지 않는다.

## 7차: 고용노동부

2026-09-02 일반 공지 https://www.moel.go.kr/news/notice/noticeList.do 와 국고보조사업 https://www.moel.go.kr/info/govsupport/govsupportcon/govSupportSubList.do?pageIndex=1 의 첫 페이지 각각 10행을 확인했다. 각각 공모 후보 2건을 추출하고 batch 1에 연결한다. 공식 상세 경로와 `bbs_seq`를 검증하며 점으로 구분된 등록일을 공고일로 저장한다. 직위공모·입찰·선정 공고·우선협상 대상자 선정은 제외한다. 보조사업 목록의 최신 행이 3월이라 일반 공지와 함께 확인하지만 과거 전체 수집이나 현재 접수 가능 여부 확인을 의미하지 않는다. 연결 기관 14개, 미연결 31개. 두 출처를 하나의 기관으로 센다.

## 6차: 식품의약품안전처

2026-09-02 공식 RSS 안내 https://www.mfds.go.kr/www/rss/list.do 에서 공고 `ntc0004` 피드를 확인하고 batch 0에 연결했다. 표본 29행에서 공모 후보 0건이다. GMT 공고일을 한국 날짜로 변환하되 접수일로 쓰지 않는다. 식약처의 `용역연구개발과제 주관연구기관 공모`는 일반 용역 제외의 좁은 예외로 유지하고 입찰·결과·채용은 제외한다. 공식 예시 https://www.mfds.go.kr/brd/m_76/view.do?seq=16316 를 근거로 하며 과거 예시를 신규 공고로 삽입하지 않는다. 연결 대상 13개, 미연결 32개. RSS 수록 범위 밖의 과거 공고 수집은 별도 작업이다.

## 5차: 산림청

2026-09-02 산림청 공식 공고(`BBSMSTR_1032`)·알립니다(`BBSMSTR_1031`) 각 첫 페이지 10행을 확인했다. 두 수집기를 batch 3에 연결한다. 본문 목록 제목 속성을 사용하고 공식 게시판 ID와 `nttId`를 검증한다. 링크의 임시 세션 식별자와 검색 파라미터를 저장하지 않는다. 현재 표본 20행의 공모 후보는 0건이며 수집 실패가 아니다. 전체 과거 공고나 기관의 모든 게시판을 포괄하지 않는다. 직접 연결 기관은 12개, 미연결은 33개다. 산림청에는 2개 출처가 연결되지만 기관 수는 1개로 센다.

## 4차: 기상청

2026-09-02 https://www.weather.go.kr/plus/rss.jsp 의 정책알림서비스 공지사항 RSS를 확인했다. `kma-board`를 batch 2에 추가한다. 공식 `gongzi` 게시판·`num` 식별자를 검증하며 채용·날씨예보 피드를 혼합하지 않는다. 표본 8행 중 공모 후보 1건을 확인했다. KST 영문 공고일을 날짜로 변환하고 제목의 이중 이스케이프 문자를 처리한다. 접수기간은 추정하지 않는다. 직접 연결 대상 11개, 미연결 34개이며 전체 과거 공고 수집을 의미하지 않는다.

## 3차: 농촌진흥청

2026-09-02 공식 사이트맵 https://www.rda.go.kr/sitemap.do 의 공지사항 링크를 확인했다. 최신 1페이지 10행에서 공모전·산업체협력연구 공모 2건을 추출했다. `rda-board`를 batch 1에 포함한다. 제목·작성일 셀과 게시판 코드 `ancmtt`, 프로그램 `nei_ancmttEntry`, `dataNo`를 검증하고 면허시험·선정결과는 제외한다. 과거 전체 목록이나 현재 접수 중 여부까지 검증한 것은 아니다. 직접 연결 대상은 10개, 미연결은 35개다.

## 2차: 질병관리청

2026-09-02 공식 정보구독 안내(https://www.kdca.go.kr/kdca/4912/subview.do)에 게시된 공고·공시 RSS 50행에서 공모 후보 27건을 확인했다. `kdca-board`를 batch 0에 연결하며 공식 `/bbs/kdca/51/{id}/artclView.do` 링크만 허용한다. 국민체험단·연수생·면허시험·자격시험은 제외한다. 이전 공고와 재공고가 포함되므로 27개 신규 사업이나 현재 접수 중인 사업 수를 뜻하지 않는다. 접수기간은 원문 확인 상태로 보존한다. 직접 연결 대상은 기존 8개에서 9개로 확대하며 나머지 36개는 미연결이다.

2026-09-02. 기존 4부처에 중소벤처기업부·농림축산식품부·법제처·보건복지부의 수집기를 추가한다. 전체 45개 기관 연결 완료는 아니다.

공식 경로:
- 중기부 RSS 안내: https://tv.mss.go.kr/site/smba/contents/view.do?menuCd=20905000000002024122902&siteCd=smba
- 농식품부 RSS 안내: https://www.mafra.go.kr/home/5327/subview.do
- 법제처 목록 내 RSS 버튼: https://www.moleg.go.kr/menu.es?bid=0010&mid=a10504000000
- 복지부 공식 공지 목록: https://www.mohw.go.kr/menu.es?mid=a10501010000

각 기존 batch 0~3에 한 출처씩 배치해 수동·정기 수집에 함께 포함한다. 목록 구조와 공식 도메인·게시판 식별자를 검증한 뒤에만 성공 처리한다. 공모 후보 0건과 구조 오류를 구분한다. 등록일은 공고일로 저장하며 접수기간으로 추정하지 않는다. 채용·공무원 전입·입찰·용역·결과·포상 등은 제외한다. 목록 수록 범위만 수집하며 과거 전체 페이지 수집 또는 상세 접수기간 보강을 의미하지 않는다.

실제 공식 표본: 중기부 RSS 20행, 농식품부 RSS 50행, 법제처 RSS 49행, 복지부 목록 15행. 법제처·복지부는 이 표본에서 필터 통과 공모 0건이다. 과기정통부 홈페이지는 HTTP 400 Request Blocked 응답으로 보류하며 차단을 우회하지 않는다.

다음 범위: 미연결 기관별 공식 게시판 추가 확인, RSS 없는 사이트 개별 파서, 페이지 범위 확대와 본문 접수기간 보강. 기관 등록 45개와 연결 상태는 API에서 별도로 확인한다.

## 16차: 국가보훈부·소방청 공식 공지사항

국가보훈부 공지사항 https://www.mpva.go.kr/mpva/selectBbsNttList.do?bbsNo=15&key=76 을 batch 2에, 소방청 공지사항 https://www.nfa.go.kr/nfa/news/notice/ 를 batch 3에 추가했다. 보훈부는 공식 상세 경로·bbsNo=15·key=76·숫자 nttNo를 검증하고 임시 jsessionid와 검색 조건을 제거한다. 소방청은 고정 공지와 일반 공지를 함께 읽고 주석 처리된 링크는 무시하며 게시판 bbs_0000000000000009·mode=view·숫자 cntId만 허용한다. 실제 목록에서 각각 10행/후보 1건, 30행/후보 1건을 확인했다. 창업 경진대회와 소방안전 공모전을 선별하고 대행업체 모집·채용·자격시험·선정결과는 제외한다. 게시일만 확인된 상태이므로 접수기간은 원문 확인으로 남긴다. 첫 페이지 연결이며 기관의 전체 과거 공고 확보를 의미하지 않는다. 우주항공청은 HTTP 400 Request Blocked로 연결을 보류했으며 우회하지 않았다.

## 15차: 인사혁신처 공지사항 RSS

공식 공지 목록 https://www.mpm.go.kr/mpm/comm/newsInnoNotice/?pageIdx=1 에서 공개한 RSS를 batch 1에 추가했다. 공식 /board/board.do, boardId=bbs_0000000000000020, mode=view, 숫자 cntId를 검증한다. 피드 100행에서 2025·2026년 문학상·미술전 작품 공모 4건을 확인했다. 공무원 미술전 작품 공모 제목에만 채용 키워드 예외를 적용하며 채용·위원·정책단·기자단·인플루언서 모집은 제외한다. RSS의 접수기간 라벨, 시작·종료일과 마감 시각을 읽고, 두 자리 연도는 제목의 명시 연도와 일치할 때만 인정한다. 4건 모두 마감으로 저장하며 접수 중으로 안내하지 않는다. RSS 100건은 전체 과거 목록을 의미하지 않는다.

## 14차: 산업통상부 사업공고

공식 목록 https://www.motir.go.kr/kor/article/ATCL2826a2625 를 batch 0에 추가했다. 목록의 article.view 호출에서 숫자 ID만 추출하며 스크립트는 실행하지 않는다. 공식 상세 경로 /kor/article/ATCL2826a2625/숫자/view 를 검증했다. 최신 10행 중 사업·지원대상과제 공고 7건을 선별하며 민간자격 등록폐지, 규제특례 승인, 선정 공고는 제외한다. 원문 제목과 게시일은 저장하되 게시일을 접수기간으로 추정하지 않는다. 첫 페이지 연결이며 과거 전체 및 상세 접수기간 수집은 별도 작업이다. 외교부 공지 RSS는 정상 세션 이동 후에도 HTTP 403이 반환되어 연결하지 않았으며 접근 제한을 우회하지 않았다. 인사혁신처 공식 공지 RSS 주소는 확인했으나 이번 배포 범위에는 포함하지 않는다.

## 13차: 통일부 공지사항 RSS

2026-09-03 오류 원인 재현: 운영과 같은 User-Agent 및 HTML/JSON 전용 Accept로 요청하면 HTTP 200이지만 2,006바이트 오류 페이지가 반환됐다. 동일 User-Agent에서 Accept만 RSS/XML 허용으로 변경하면 정상 RSS 29,073바이트, 10행이 반환됐다. 중앙 RSS 수집기의 Accept를 형식별로 지정했으며 HTML 수집기 요청은 유지했다. HTTP 성공만으로 연결을 정상 처리하지 않는 기존 구조 검증도 유지한다. 운영 복구는 배포 후 수집 결과로 별도 확인한다.

공식 RSS 안내 https://www.unikorea.go.kr/web/unikorea/contents/Information_rss 에 공개된 공지사항 RSS를 batch 3에 추가했다. 상세 링크는 unikorea.go.kr의 /web/unikorea/bbs/bbs_0000000000000001/숫자 경로만 허용한다. +0900 게시일은 한국 날짜로 검증하며 접수기간으로 사용하지 않는다. 최신 10행은 현재 공모 선별 기준에 해당하는 후보 0건이며, 용역 공모·선정결과는 제외한다. 유효한 목록의 후보 0건과 수집 실패를 구분한다. RSS 최신 목록만 수집하며 과거 전체 공고 연결을 의미하지 않는다.

## 12차: 국가데이터처 공지사항 RSS

공식 RSS 안내 https://mods.go.kr/menu.es?mid=a10707000000 에서 공개한 공지사항 RSS를 batch 2에 추가했다. 실제 상세 링크는 공식 mods.go.kr의 board.es, bid=108, act=view, 숫자 list_no 조합만 허용한다. 최신 피드 10행에서 공모 후보 1건을 확인했다. 해당 공고는 명시된 접수기간 2026-07-20~2026-08-09 23:59(KST)를 읽어 마감으로 저장한다. 접수기간 라벨·날짜·마감 시각을 확인할 수 없거나 중복·모순이면 추정하지 않는다. 모니터단·서포터즈 모집은 제외한다. 운영 연결 성공 여부와 공모 접수 중 여부는 별개이며, 과거 전체 피드 수집을 의미하지 않는다.

## 11차: 국가유산청 공지사항

2026-09-03 연결 점검: 운영에서 bare 도메인 연결이 반복 종료되어 동일 목록을 제공하는 공식 www 주소로 수집 요청을 변경했다. 최초 등록된 DB URL이 코드의 주소 수정을 덮어쓰던 경로도 수정해 중앙 수집기는 검증된 코드 설정을 우선 사용한다. 기존 공고·연결 이력·원문 ID는 보존한다. 로컬에서는 두 주소 모두 10행/후보 1건으로 동일하며, 실제 운영 복구 여부는 별도 실행 결과로 판단한다. 프록시·인증 우회는 사용하지 않는다.

공식 목록 https://khs.go.kr/multiBbz/selectMultiBbzList.do?bbzId=newpublic&mn=NS_01_01 을 batch 1에 추가했다. 공식 도메인·newpublic 게시판·숫자 id/no를 검증하고 URL의 임시 jsessionid와 검색 조건을 제거한다. 첫 페이지 10행에서 전승공예품 악기은행 대여자 모집 후보 1건을 확인했다. 수상자 공고·평가위원 모집·선정결과는 제외한다. 공고일은 2026-08-28이며 접수기간은 원문 확인으로 유지한다. 운영 성공 확인 시 연결 기관은 18/45개다. 관세청 공식 RSS는 HTTP 400 응답으로 보류했으며 접근 제한을 우회하지 않았다.

## 10차: 법무부 공지사항

공식 목록 https://www.moj.go.kr/moj/116/subview.do 를 batch 0에 추가했다. 상세 주소는 법무부 도메인의 /bbs/moj/184/숫자/artclView.do 형태만 허용한다. 첫 페이지 10행에서 부지 선정·비자 사업·콘텐츠 공모 후보 3건을 확인했다. 수상작 발표, 자문단 모집, 현장투어 모집은 제외한다. 공고일과 접수기간을 구분하며 과거 전체 목록이나 상세 접수기간은 아직 수집하지 않는다. 운영 성공 확인 시 중앙기관 연결은 17/45개다.

## 9차: 해양수산부 공지사항

공식 목록 https://www.mof.go.kr/doc/ko/selectDocList.do?menuSeq=375&bbsSeq=9 를 batch 3에 추가했다. 목록의 공식 상세 이동 함수에서 숫자 docSeq만 추출하며 임의 스크립트는 실행하지 않는다. 고정 공지 2건을 포함한 12행에서 공모·지원계획 후보 4건을 확인했다. 신규과제 선정계획과 사업대상지 선정 연장 공고를 인식하되 선정결과, 조사 사업자 선정, 위원·채용 모집은 제외한다. 공고일만 저장하고 접수기간은 원문 확인으로 유지한다. 현재 첫 페이지 수집이며 채용·공모 게시판과 과거 전체 페이지는 포함하지 않는다. 운영 성공 확인 시 중앙기관 연결은 16/45개다.

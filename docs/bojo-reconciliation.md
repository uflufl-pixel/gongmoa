# 2026-09-02 국고 포털 대조

- API 최초 전체 스캔: 394페이지, 원본 196,595행 완료.
- 공개 검색 목록 `/retrieveSearchPubBiz.do`를 국고(1), 공모기간 2026-01-01~2026-12-31 조건으로 257페이지 조회했다. 총 2,563개 공고 식별자를 확인했으며 페이지별 행수·식별자 중복·전체 건수 변화를 검사했다.
- 종료 표시가 없고 접수 종료일이 남은 목록 65건 중 저장된 ID와 일치 36건, 미일치 29건. 저장된 BOJO 공고 40건의 ID는 모두 위 연간 목록 안에 있었다.
- 목록의 접수 상태는 상세 상태와 다를 수 있다. 29건을 곧바로 접수 중이라고 단정하지 않고 원문 상세를 확인한다. 식별자 스냅샷은 data/bojo-audit-2026-09-02.json에 기록했다.
- 공식 화면의 IA005100.js는 `/da/retrieveTaskReqstPopAjax.do`를 POST bsnsyear,nttId로 조회하며 상태 0(오늘 마감),1(접수 중),2(접수 마감),기타(종료)로 표시한다.
- `/api/bojo-reconcile`은 {ids:[공식ID]}를 최대 5개 받아 서버에서 위 공식 원문을 재조회한다. ID·국고 구분·필수 항목·접수 상태가 확인된 공고만 기존 ID 기반으로 보완 저장한다. 입력자가 제목·날짜·예산이나 임의 URL을 주입할 수 없다.
- 원문의 공고기관을 소관부처로 추정하지 않는다. 기존 소관부처는 보존하고 신규 기관 유형은 미확인으로 둔다. 실패·종료는 결과로 반환하며 기존 자료를 삭제하지 않는다.
- 이 조회 경로는 공개 화면이 사용하는 내부 조회 경로로, 공공데이터 OpenAPI와 같은 안정성 계약이 확인된 것은 아니다. 구조 변화 감시 및 재대조의 정기 자동화는 후속 작업이다.
- 해당 연간 공모기간의 비교이며 전국 모든 정부 공모, 전년도 이월, 지방 공모의 완전성을 보증하지 않는다. API와 포털 불일치의 근본 원인이 제공 범위·갱신 지연·결합 조건 중 무엇인지는 미확인이다.

## 2026-09-05 재대조 결과

- 운영 검색 응답과 2026-09-02 `missingIds` 29개를 다시 비교했다. 4개(`202600000571530400005A`, `202600000570304600001A`, `202600000576176600004A`, `202632000000013200019B`)는 이미 `bojo` 레코드로 존재했다.
- 나머지 25개는 공식 `/da/retrieveTaskReqstPopAjax.do`를 저장 없이 조회했다. 모두 식별자와 국고 구분이 일치했으나 공식 접수상태가 `2`(접수 마감)였다. HTTP 실패·구조 오류는 0건이다.
- 따라서 조정 API POST를 실행하지 않았고 신규·변경·마감 영구저장은 모두 0건이다. 9월2일의 미일치29건을 현재 신규29건으로 보지 않는다.
- 현재 조정 API는 항목별 즉시 저장하므로 혼합 묶음에서 뒤 항목이 실패해도 앞 성공은 남는다. 앞으로도 실행 전 현재 DB 대조와 공식 원문 선조회를 거치고, 성공 ID만 재실행하지 않는다.
- 이 스냅샷 범위의 미일치 처리는 완료했다. 다음 확대는 투어라즈 타기관 행의 실제 발행기관·개편대기 여부를 매핑하며, 한국관광공사 공고로 일괄 귀속하지 않는다.
